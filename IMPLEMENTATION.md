# 实施规范：Codex 项目上下文工程 v1

## 1. 目标

建立一套跨项目复用、低维护成本、可逐步迭代的 Codex 项目上下文工程，使用户在日常开发中只需描述任务，而不必反复提醒 Agent：

- 读取哪些上下文；
- 如何判断任务风险；
- 何时测试；
- 何时更新项目状态、决策或文档；
- 如何安全提交 Git。

核心原则：

> 代码变化默认由 Git 保存；只有知识变化才进入项目上下文文件。

## 2. 组件架构

```text
个人 Codex 环境
├── ~/.codex/AGENTS.md
└── $HOME/.agents/skills/
    ├── project-context-init/
    └── project-task-finalize/

项目仓库
├── AGENTS.md
├── README.md
├── .gitignore
├── .ai/
│   ├── current-state.md
│   └── decisions/
└── docs/
```

### 职责边界

| 组件 | 负责 | 不负责 |
|---|---|---|
| 全局 `AGENTS.md` | 个人稳定工作原则、通用风险边界 | 具体项目知识、冗长流程 |
| 项目 `AGENTS.md` | 项目命令、项目特有规则、上下文路由 | 当前任务、历史流水账 |
| Skill | 跨项目复用的多步骤智能流程 | 具体项目事实 |
| 脚本 | 目录创建、Git 状态收集等确定性操作 | 任务分级、知识价值判断 |
| `.ai/current-state.md` | 当前项目快照 | 提交历史、开发日志 |
| `.ai/decisions/` | 已确认的长期决策 | 普通实现细节 |
| `docs/` | 代码无法充分表达且未来反复需要的知识 | 逐文件代码说明 |
| Git | 差异、历史、恢复与原子提交 | 解释设计理由 |
| Codex | 理解、分级、执行、验证与语义判断 | 绕过规则或原生审批 |
| 用户 | 确认高风险方案与长期规则 | 记忆日常维护步骤 |

## 3. 分层上下文

### 始终加载

- `~/.codex/AGENTS.md`；
- 当前路径适用的项目 `AGENTS.md`。

### 重要功能任务加载

- `.ai/current-state.md`；
- 与任务直接相关的代码和配置。

### 按需加载

- 相关 `.ai/decisions/*.md`；
- 相关 `docs/*.md`。

不默认读取全部项目文档。只有出现真实模块差异时，才在相应子目录添加更具体的 `AGENTS.md` 或 `AGENTS.override.md`。

## 4. 任务风险模型

Agent 按实际影响自主判断，而不是让用户手工标记等级。

### Level 1：局部、易回滚

例：文案、样式、简单 Bug、局部重构。

- 可直接执行；
- 运行最小必要验证；
- 通常不更新上下文。

### Level 2：功能或跨模块变化

例：新增 API、重要功能、多个文件的逻辑调整。

- 先给出简短实施方案；
- 无关键歧义时可继续执行；
- 运行相关测试；
- 检查是否更新当前状态或项目文档。

### Level 3：高影响、难回滚或外部副作用

例：数据库迁移、认证权限、公开接口、核心依赖、生产环境、删除重要模块。

- 说明方案、影响、验证和恢复方式；
- 修改前获得用户确认；
- 必要时创建独立分支；
- 完整相关验证；
- 记录已确认的长期决策。

执行中发现影响扩大时必须升级等级；升级到 Level 3 后暂停并重新确认。

## 5. 项目上下文文件

### `.ai/current-state.md`

保存项目当前快照：阶段、目标、已完成能力、正在进行、已知问题、下一步。

- 状态变化时替换过期内容；
- 不追加提交流水；
- 不重复代码与 Git 已表达的信息；
- Agent 可自动更新。

### `.ai/decisions/`

只记录用户已经确认、会影响未来工作的长期决策。

- 使用递增编号；
- 既有决策不直接改写历史；
- 被替代时新建决策并引用旧文件；
- 决策本身需先确认，确认后 Agent 可自动落稿。

### `docs/`

只有同时满足以下条件才创建或更新：

1. 信息无法稳定地从代码、配置或 Git 获得；
2. 缺失可能导致未来 Agent 做出错误判断；
3. 未来有明确的重复读取场景。

### `AGENTS.md`

只在长期工作规则、真实命令、风险边界或上下文路由发生变化时修改。Agent 可以提出建议，但未经用户确认不自动修改。

## 6. Git 工作流

标准流程：

```text
记录工作区基线
→ 修改
→ 验证
→ 上下文检查
→ 检查最终 diff
→ 路径级暂存
→ 本地原子 commit
```

规则：

- 默认允许自动创建本地 commit；
- 一个完整任务对应一个原子 commit；
- 不自动 push、merge、rebase、deploy 或改写历史；
- 不自动 stash、clean、reset 或还原用户既有修改；
- 有无关修改时使用路径级暂存；同文件无法安全分离时停止；
- 回滚已提交变更优先使用 `git revert`；
- 不使用 `--no-verify` 绕过 Hook；
- Level 3 默认使用 `codex/<short-task-name>` 分支，但不得破坏现有工作区。

## 7. 测试与验证

验证命令来源优先级：

1. 项目 `AGENTS.md`；
2. 项目自动化配置和 CI；
3. README/贡献指南；
4. 现有脚本与测试目录；
5. 技术栈标准命令。

验证原则：

- 运行与风险和影响范围匹配的“最小但充分”验证；
- 改变可验证行为时优先补充测试；
- Bug 修复优先添加回归测试；
- 不删除、跳过或弱化测试以掩盖失败；
- 相关验证失败时不创建完成性 commit；
- 环境无法验证时区分代码失败、依赖缺失、外部服务不可用和权限限制，并明确未覆盖范围。

## 8. 安全与审批边界

Codex 原生沙盒与审批控制“技术上能否执行”；本系统控制“项目上是否应该执行”。

默认自主：工作区内普通代码/文档修改、测试、Git 查看和符合规则的本地提交。

必须确认：

- 数据库结构、真实数据迁移；
- 认证、授权、安全边界；
- 公开接口、核心架构、核心生产依赖；
- 生产配置、部署、外部资源或付费操作；
- push、merge、发布；
- 修改全局或项目长期规则；
- 删除长期文档或重要模块。

禁止自主：

- 绕过 Codex 审批、测试或 Hook；
- 强制推送、`git reset --hard`；
- 覆盖无法确认归属的用户修改；
- 读取、输出或提交真实密钥；
- 将项目代码、数据或凭据发送给未批准的外部服务。

## 9. 两个全局 Skill

### `project-context-init`

显式调用，只执行一次项目初始化：

- 安全创建最小结构；
- 分析已有代码和配置；
- 填写可确认的项目目标、命令和状态；
- 不创建推测性架构/API/任务文档；
- 显示 diff；
- 创建初始化 commit；
- 提醒用户新开 Codex 会话。

### `project-task-finalize`

允许隐式调用，用于产生文件修改的任务收尾：

- 收集 Git 证据；
- 检查目标与修改范围；
- 运行或核对适当验证；
- 判断是否更新状态、决策或文档；
- 检查敏感操作和无关变更；
- 精确暂存并创建本地原子 commit；
- 汇报结果、验证、上下文变化、commit 和限制。

## 10. 初始化后的会话边界

Codex 在会话开始时发现并加载 `AGENTS.md`。因此 `$project-context-init` 创建或修改项目 `AGENTS.md` 后：

1. 完成初始化 commit；
2. 结束当前 Codex 会话；
3. 从项目根目录启动新会话；
4. 要求 Codex列出已加载指令源；
5. 再开始正式任务。

## 11. 第一版有意延后

- MCP；
- Plugin 打包；
- Stop Hook 和新 Git Hook；
- 多 Agent；
- Git worktree；
- 自动 push/PR/发布；
- 外部知识库；
- 多种技术栈模板；
- 独立测试或代码审查 Skill。

真实实践中只有出现重复问题后才增加：

- Skill 经常漏触发：增加 Stop Hook；
- 多设备/多人分发：打包 Plugin；
- 需要外部系统：引入 MCP；
- 模块规则真实不同：增加局部 AGENTS；
- 相同流程多次重复：新增 Skill。

## 12. 验收标准

安装完成后应满足：

- `~/.codex/AGENTS.md` 保留原有内容并加入必要规则；
- 两个 Skill 可通过 `/skills` 或 `$` 发现；
- 初始化 Skill 不会覆盖现有非空文件；
- 初始化项目会生成最小上下文结构；
- 新会话能加载项目 `AGENTS.md`；
- 一个简单任务可经过验证、上下文检查和本地 commit；
- 无关工作区修改不会被一起提交；
- 高风险测试场景会在修改前请求确认。
